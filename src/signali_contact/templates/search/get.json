{% load restful %}{% for result in results %}[
    {
        "title": {{result.meta.title | jsonify | safe}},
        "description": {{result.description | jsonify | safe}},
        "url": "{% url 'contact-point' slug=result.meta.slug %}",
        "score": {{result.watson_rank | jsonify | safe}}
    }
]{% endfor %}
